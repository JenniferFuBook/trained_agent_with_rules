First, add {node}, then connect it to {node_a}. 
Create a new node {node} and connect it to {node_a} and {node_b}.
Add {node}, delete {node_a}, then connect {node_b} to {node_c}.
Please add {node} and {node_a}, then disconnect {node_b} from {node_c}.
Create {node}, connect it to {node_a}, and then remove {node_b}.
Add nodes {node} and {node_a}, connect them, and then delete {node_b}.
Insert {node} and connect it to {node_a} and {node_b}.
Create {node}, disconnect it from {node_a}, and then add {node_b}.
Add {node}, delete {node_a}, add {node_b}, and connect {node_b} to {node_c}.
First add {node}, then connect {node_a} and {node_b}, and finally remove {node_c}.